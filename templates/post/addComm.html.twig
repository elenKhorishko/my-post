{% extends 'base.html.twig' %}

{% block body %}

    {% for message in app.flashes('comm') %}
        <p>{{ message }}</p>
    {% endfor %}

    <h1>Добавить новый комментарий</h1>
    <div class="container-fluid">


        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-default">Добавить</button>
        {{ form_end(form) }}


    </div>
{% endblock %}

